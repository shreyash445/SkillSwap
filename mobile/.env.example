# Optional — only needed if auto-detection of your dev machine's IP fails.
# Copy to `.env` and set your machine's LAN IP. The phone must reach this IP.
EXPO_PUBLIC_API_URL=http://192.168.1.10:8000/api